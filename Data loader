# data_loader.py
import sqlite3
import pandas as pd

def load_data(db_path):
    conn = sqlite3.connect(db_path)
    df = pd.read_sql_query("SELECT * FROM business_metrics", conn)
    conn.close()
    return df
